my_postgres_project:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('DB_HOST', 'host.docker.internal') }}"
      user: postgres
      password: "123"
      port: 5432
      dbname: postgres
      schema: public
      threads: 1

    ci:
      type: postgres
      host: "127.0.0.1"
      user: postgres
      password: "123"
      port: 5432
      dbname: postgres
      schema: public
      threads: 1