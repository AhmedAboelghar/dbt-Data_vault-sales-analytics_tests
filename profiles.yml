my_postgres_project:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('DB_HOST', 'host.docker.internal') }}"  # <--- التعديل هنا
      user: postgres
      password: "123"
      port: 5432
      dbname: postgres
      schema: public
      threads: 1